import * as config from '../config/config';

export class appService {
    
    getNewsDetailsData = async()=>{
        let output_data = {message:[],status:200}
        try{

        } catch(error){

        }

        return output_data;
    }
}